<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.edit.EditActivity">

    <art.intel.soft.view.BaseToolbar
        android:id="@+id/navigationTb"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <FrameLayout
        android:id="@+id/main_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/editPanelBackground"
        app:layout_constraintBottom_toTopOf="@+id/toolbar_fragment"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/navigationTb">

        <ja.burhanrashid52.photoeditor.view.PhotoEditorView
            android:id="@+id/photo_editor_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <com.canhub.cropper.CropImageView
            android:id="@+id/crop_iv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            app:cropBorderCornerColor="@color/colorAccent"
            app:cropBorderLineColor="@color/colorAccent"
            app:cropBorderLineThickness="2dp"
            app:cropGuidelines="on"
            app:cropGuidelinesColor="@color/colorAccent"
            android:visibility="gone"
            app:cropBorderCornerOffset="1dp"
            app:cropBorderCornerLength="15dp"
            app:cropBorderCornerThickness="3dp"
            tools:visibility="visible"/>

        <FrameLayout
            android:id="@+id/container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:minWidth="0dp"
            android:minHeight="0dp"
            android:visibility="gone">

        </FrameLayout>

        <ImageView
            android:id="@+id/support_iv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

    </FrameLayout>

    <art.intel.soft.view.SelectiveColorView
        android:id="@+id/selective_color_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/colorAlphaWhite"
        android:visibility="gone"
        app:layout_constraintBottom_toTopOf="@+id/toolbar_fragment" />

    <LinearLayout
        android:id="@+id/before_after_btn"
        android:layout_width="45dp"
        android:layout_height="45dp"
        android:layout_margin="@dimen/margin_large"
        android:padding="0dp"
        android:visibility="gone"
        android:gravity="center"
        app:layout_constraintBottom_toTopOf="@+id/selective_color_view"
        app:layout_constraintEnd_toEndOf="parent"
        tools:visibility="visible">

        <ImageView
            android:layout_width="30dp"
            android:layout_height="25dp"
            android:src="@drawable/ic_before_after" />

    </LinearLayout>

    <FrameLayout
        android:id="@+id/toolbar_fragment"
        android:background="@android:color/transparent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
