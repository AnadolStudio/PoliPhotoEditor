<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:gravity="center"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/select_image_container"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:padding="8dp"
        app:layout_constraintBottom_toTopOf="@+id/stickers_recycler_view"
        app:layout_constraintStart_toStartOf="parent">

        <ImageView
            android:id="@+id/select_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:src="@drawable/ic_sticker_group_0"/>

    </FrameLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/stickers_group_recycler_view"
        android:layout_width="0dp"
        android:layout_height="40dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintBottom_toTopOf="@+id/stickers_recycler_view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/select_image_container"
        tools:orientation="horizontal" />

    <androidx.constraintlayout.widget.Group
        android:id="@+id/stickers_group_group"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:visibility="gone"
        tools:visibility="visible"
        app:constraint_referenced_ids="select_image_container,stickers_group_recycler_view"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/stickers_recycler_view"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/bottom_card_view_height"
        android:layout_gravity="center"
        android:orientation="horizontal"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        tools:listitem="@layout/item_image_list" />

</androidx.constraintlayout.widget.ConstraintLayout>
